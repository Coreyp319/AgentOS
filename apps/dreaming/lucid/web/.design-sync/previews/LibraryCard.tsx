import { LibraryCard } from 'web'

// The saved-dream management card: a "kept on this computer" note with an inline
// Rename, plus the two-step Delete. (The rename editor and armed-delete are
// interaction-only states, shown live in the app.)
export const Default = () => <LibraryCard name="Aurora over still water" />
