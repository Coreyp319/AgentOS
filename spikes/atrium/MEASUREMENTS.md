# Atrium spike — measurements

_Generated by `measure.py`. Re-run to refresh. Contrast / weight / RSS / still-room are environment-independent; headless GPU-frame numbers are software-GL and flagged._

## 1. Degraded-state WCAG contrast (ADR-0031 gap #1 — the live a11y cap)

| stale treatment | ink (fg) | paper (bg) | contrast | AA text (4.5) | AA large (3.0) |
|---|---|---|---|---|---|
| none (live) | rgb(230, 233, 240) | rgb(34, 36, 47) | **12.69:1** | ✅ | ✅ |
| **ADR-0031 fix** (dim signal, keep text) | rgb(230, 233, 240) | rgb(31, 34, 46) | **13.03:1** | ✅ | ✅ |
| original (`opacity:.5;saturate(.6)` on text) | rgb(129, 133, 143) | rgb(29, 33, 47) | **4.34:1** | ❌ | ✅ |

The fix keeps body text at full contrast while signalling 'blind' through the dots/glass; the original treatment dims the text itself.

### Secondary inks over glass (live state)

| token | ink | contrast | AA text (4.5) | AA large (3.0) |
|---|---|---|---|---|
| --inst-muted (#8a90a0) — descriptions | rgb(138, 144, 160) | **4.84:1** | ✅ | ✅ |
| --inst-label (#878c9b, lifted from #7a8090) — labels/footer | rgb(135, 140, 155) | **4.55:1** | ✅ | ✅ |

Secondary inks clear AA at body size (`--inst-label` lifted #7a8090→#878c9b across the shared instrument register — ADR-0031).

## 2. PWA install readiness (gap #5)

- manifest present: True · service worker: True
- required fields missing: none
- 512px icon: True · maskable icon: True
  - `/icons/icon-192.png` declared 192x192 → real 192x192 (any) ✅
  - `/icons/icon-512.png` declared 512x512 → real 512x512 (any) ✅
  - `/icons/icon-512-maskable.png` declared 512x512 → real 512x512 (maskable) ✅
- **static installability check: ✅ PASS**
- _gate residual: install over the **actual** `tailscale serve` HTTPS origin (SW scope under the proxy) must be confirmed on the box — static checks pass; live origin is owed._

## 3. Standing-process cost (Open Q1 / ADR-0026 idle-exit)

- **still room:** WebGL=False · canvas=False · rAF=False → _still room (no GPU backdrop)_ (≈0 GPU; no reactive backdrop)
- host server idle RSS: **27.2 MiB**
- `/launch.json` latency (local): min 0.5 ms · median 0.5 ms
- **marginal new standing processes when folded into the status panel: 0** (the view is a route on the existing daemon)

## 4. First-paint determinant + weight

- launch-view shell: **23432 bytes** of HTML (inline CSS+JS) · icons 26329 bytes (cached after install)
- requests blocking first paint: **1** (the document) · external blocking resources: **0**
- headless full load+screenshot reference: 375.0 ms (software GL; loose ceiling)
- _end-to-end chromium spawn+load+screenshot on software GL; a loose ceiling, not FCP. Real FCP needs the box — but the page is 1 request, inline CSS/JS, no web fonts, no WebGL ⇒ paint is parse-bound._

