/target
**/target
*.swp
.DS_Store
# throwaway external clone + its venv (C1-spike); findings live in spikes/kwin-mcp-FINDINGS.md
/spikes/kwin-mcp/
**/.venv/

# Python bytecode (Hermes plugin source)
__pycache__/
*.pyc

# transient git worktrees (e.g. the act-verbs worktree) — never commit a nested working tree
/.claude/worktrees/

# editor/tool backups (DesignSync *.orig conflict-origins, *.bak) — never commit
*.orig
*.bak

# compiled spike binaries — rebuild from the in-place .c sources (build.sh), never commit ELF
/spikes/ue-probe/sdl3_vulkan/sdl3_vulkan_wallpaper
/spikes/ue-probe/ue_wallpaper/libagentos_layershell.so
/spikes/ue-probe/wallpaper_role/wallpaper_role_plasma
